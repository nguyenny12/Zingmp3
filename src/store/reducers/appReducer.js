import actionType from '../actions/actionType';

const initState = {};
const appReducer = (state = initState, action) => {
	switch (action.type) {
		case value:
			return state;
		default:
			return state;
	}
};
